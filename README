######################################################################################
# 원 Core UI Directory 구조
######################################################################################
coreui-free-vue-admin-template
├── public/          # static files
├── src/             # project root
│   ├── assets/      # images, icons, etc.
│   ├── components/  # common components - header, footer, sidebar, etc.
│   ├── layouts/     # layout containers
│   ├── scss/        # scss styles
│   ├── router       # routes config
│   ├── stores/      # template state example 
│   ├── views/       # application views
│   ├── _nav.js      # sidebar navigation config
│   ├── App.vue
│   ├── ...
│   └── main.js
├── index.html   # html template
├── package.json
└── vite.config.mjs

######################################################################################
# Core UI + Typescript Directory 구조
######################################################################################
project-frontend-coding/
├── package.json
├── tsconfig.json          👈 TS 설정
├── vite.config.(js|ts)
└── src/
    ├── main.ts            👈 진입점 (TS)
    ├── env.d.ts           👈 .vue 모듈 선언 (TS용)
    ├── App.vue            👈 Vue SFC (TS로 변경 가능)
    ├── _nav.js            👈 CoreUI 메뉴 정의 (JS 그대로)
    ├── router/
    │   └── index.js       👈 JS 그대로 (vue-router)
    ├── assets/
    │   ├── icons/
    │   │   └── index.js   👈 JS 그대로 (iconsSet)
    │   └── ...
    ├── components/
    │   ├── DocsComponents.js  👈 JS 그대로
    │   ├── DocsExample.js     👈 JS 그대로
    │   ├── DocsIcons.js       👈 JS 그대로
    │   └── ...
    └── views/
        ├── Dashboard.vue  👈 여기부터 TS로 바꿔가도 됨
        └── ...            👈 새로 만드는 컴포넌트는 lang="ts"

######################################################################################
# Typescript 사용
######################################################################################
npm install -D typescript vue-tsc @types/node

#########################################################
# /tsconfig.json 추가
#########################################################

{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",

    "allowJs": true,        // JS import 허용
    "checkJs": false,       // JS 타입체크는 안 함 (그냥 any)
    "noEmit": true,         // tsc가 JS/TS 파일을 따로 출력하지 않게

    "strict": true,
    "noImplicitAny": false, // JS에서 오는 any 허용 (경고 줄이기)

    "jsx": "preserve",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "lib": ["ESNext", "DOM"],
    "types": ["vite/client"],

    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts",
    "src/**/*.tsx",
    "src/**/*.vue"
    // ❌ JS는 include 안 함 (allowJs로 import만 허용)
  ]
}

#########################################################
# env.d.ts 설정 (.vue 인식용)  src/env.d.ts 파일
#########################################################
/// <reference types="vite/client" />

declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const component: DefineComponent<{}, {}, any>
  export default component
}

